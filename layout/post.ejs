<% if (page.tags.length > 0) { %>
  <% page.tags.slice(0, 1).each(tag => { %>
    <%- partial('./components/main', {
      showTag: tag.name,
      from: 'post'
    }) %>
  <% }) %>
<% } else { %>
  <%- partial('./components/main', {
    showTag: '',
    from: 'post'
  }) %>
<% } %>


